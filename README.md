# 日本の公開データ研究マップ

日本の公的統計を使った研究論文を集め、テーマ・データ源・解析データの年・研究の型で探せるようにした知見マップである。
収載は 1,091本（2026年9月24日時点）。分類は TypeSafe の判定モデル Jev による機械判定で、人手による確認は行っていない。

サイト: https://shoei05.github.io/JapanStatsMAPs/

## 収載範囲

主解析のデータが次のどちらかに当たる論文を収載した。

| 区分 | 定義 | 本数 |
|---|---|---|
| A 公開集計データ | 誰でもダウンロードできる公表集計（NDBオープンデータ、e-Stat の統計表、人口動態統計の公表表など） | 626 |
| B 政府統計の個票 | 二次利用申請で得た政府統計調査の個票（国民生活基礎調査、国民健康・栄養調査、患者調査、人口動態調査の死亡票など） | 453 |
| A＋B 併用 | A と B を組み合わせたもの | 12 |

NDB 特別抽出、DPC、商用レセプト、病院の記録、独自のコホートや調査は対象外とした。公表値を人口の分母や年齢調整の基準としてだけ使い、主解析が別のデータである論文も対象外である。

## データ源

次の15種類について検索した。1本の論文が複数のデータ源を使う場合は、それぞれに数える。

| データ源 | 収載論文 |
|---|---|
| 人口動態統計 | 321 |
| 国民生活基礎調査 | 188 |
| 国民健康・栄養調査 | 177 |
| NDBオープンデータ | 171 |
| e-Stat・公表統計表 | 119 |
| 医療施設調査・医師統計 | 109 |
| 患者調査 | 35 |
| 全国家計構造調査・全国消費実態調査 | 34 |
| 国勢調査 | 28 |
| 乳幼児身体発育調査・学校保健統計 | 21 |
| 中高年者縦断調査 | 21 |
| 歯科疾患実態調査 | 9 |
| 21世紀出生児縦断調査 | 6 |
| 社会生活基本調査 | 6 |
| 就業構造基本調査 | 4 |

区分は A・B と判定されたものの、どのデータ源かを特定できなかった論文が 56本ある（画面では「特定不能」）。

## 方法

1. **収集。** Europe PMC で「データ源の英語名 AND (Japan OR Japanese)」を本文まで含めて検索した（MEDLINE と PMC 収載分）。データ源の名称は方法欄にだけ書かれることが多く、題名・抄録だけの検索では取りこぼすためである。重複を除いて 6,873本。
2. **一次判定。** 題名と抄録を Jev に渡し、区分（A／B／A＋B／範囲外／実証研究でない／判定不能）を判定した。A・B・A＋B・判定不能の 1,546本を候補とした。
3. **本文の取得。** 候補のうちオープンアクセスの論文は Europe PMC から全文を取得し、方法欄と結果欄を抜き出した。755本は本文で、336本は題名と抄録で判定した（収載論文の内訳）。
4. **詳細判定。** 候補の全件について、次節の項目を Jev で判定した。1本あたり約75問を1回の問い合わせで聞いている。
5. **テーマの追加。** テーマ未分類の論文と、主曝露・主アウトカムが既存テーマに当たらない論文から新テーマの候補を立て、Jev に「その概念の論文中の役割」と「既存テーマで足りるか」を判定させた。多数が新テーマを要するとした5つ（気象・環境曝露、環境負荷・脱炭素、障害・生活機能、生活時間・余暇、経済・産業）を加え、全件を判定し直した。
6. **線の確認。** 問いの型が「関連の検討」または「制度・出来事の評価」の論文について、主曝露→主アウトカムの組合せを実際に解析したかを Jev に判定させた。「実際に解析した」ものだけを線として残し、38件（調整のみ、背景のみ、判定不能）を外した。
7. **論文のつながり。** OpenAlex から参考文献リストと被引用数を取得し、参考文献の重なり（書誌結合）、直接引用、共引用、テーマの重なりから、論文どうしの近さを計算した。参考文献を取得できた論文は 995本、テーマの重なりだけで結んだ論文は 96本。

## 判定項目

| 項目 | 内容 |
|---|---|
| 区分 | A／B／A＋B（収載外の区分を含めて判定） |
| データ源 | 検索に当たった各データ源を、解析に使ったか（確率 0.5 以上を使用とみなす） |
| 解析データの年 | 解析に用いたデータの最初の年と最後の年 |
| 問いの型 | 関連の検討、複数要因の探索、実態の記述、推移、地域差、制度・出来事の評価、尺度の検証、方法の検討、その他 |
| デザイン | 横断、反復横断、縦断、生態学的研究、時系列、尺度の検証、方法論、その他 |
| 対象集団 | 国民全体、成人、高齢者、子ども、新生児・出生、女性、妊産婦、就労者、患者、医療従事者・医療機関、世帯、その他 |
| 分析単位 | 個人、世帯、都道府県、市区町村・二次医療圏、全国集計、医療機関、その他 |
| 主曝露・主アウトカム | 63テーマのどれに当たるか（役割なし、該当テーマなし、判定不能を含む） |
| テーマ | 63テーマそれぞれについて、曝露・アウトカム・記述の対象・媒介・効果修飾・層別のいずれかとして扱ったか（調整変数のみは含めない） |
| 効果推定値の報告 | 信頼区間付きの推定値が報告されているか |

63テーマは13のグループにまとめて表示している（依存・嗜好、デジタル・情報、精神・心理、人間関係・社会的経験、感染症・予防、医療利用・疾病管理、生活習慣・身体健康、妊娠・家族・性、就労・社会経済・地域、疾病・死亡、医療提供・医療資源、人口・発育、環境・経済）。定義は `data/schema_od_v2.json` にある。

## 画面

| タブ | 内容 |
|---|---|
| 概念マップ | 円はテーマ（大きさはそのテーマを扱う論文数）、線は確認済みの主曝露→主アウトカム（太さは論文数） |
| 関連の行列 | 曝露テーマ×アウトカムテーマの論文数 |
| データ源と年 | データ源ごとに、解析データの年の分布を重ねた図 |
| 論文一覧 | 条件に合う全論文。区分・データ源・データの年・判定に使った本文を表示 |
| 論文のつながり | 起点の論文と、参考文献やテーマが近い論文 |
| 統計・データ | 収集と判定の件数、各項目の内訳、方法、謝辞 |

配色は The Grand Budapest Hotel を基調とした Wes Anderson のパレット（R パッケージ wesanderson）から選んだ。既定は明るい画面で、右上のボタンで暗い画面に切り替えられる。

## ファイル

| パス | 内容 |
|---|---|
| `index.html` | サイト本体（1ファイル。`viewer/index.html` と同じもの。CSV と SVG の保存ができる） |
| `viewer/data.json` | 画面に埋め込んだデータ |
| `data/records_bib.jsonl` | 収集した 6,873本の書誌（抄録は著作権のため除いた。`scripts/fetch_epmc.py` を実行すると抄録付きの `data/records.jsonl` を作り直せる） |
| `data/jev_judgments.jsonl` | 一次判定の結果 |
| `data/structured_v3.jsonl` | 詳細判定の結果 |
| `data/new_domain_review.json` | 新テーマ候補の判定 |
| `data/jev_followup.json` | 線の確認と、該当テーマなしの再判定 |
| `data/unmapped_resolution.json` | 該当テーマなしの再判定のうち採用したもの（題名・抄録と照合） |
| `data/paper_reviews.json` | Claude（生成AI）が抄録を読んでテーマを割り当てた5本と、その根拠 |
| `output/judgments_all.csv` | 一次判定の全件一覧 |
| `output/review_sheet_v2.csv` | 人手点検用の抽出表（区分ごと20本） |

## 作り直す手順

```sh
python3 scripts/fetch_epmc.py          # 収集（data/epmc_cache/ に検索結果を保存）
zsh -lc 'python3 scripts/judge_jev.py' # 一次判定
python3 scripts/fetch_fulltext.py      # 本文の取得
python3 scripts/fetch_openalex.py      # 参考文献と被引用数
zsh -lc 'python3 scripts/structure_v2.py --workers 12'  # 詳細判定
zsh -lc 'python3 scripts/followup_jev.py'               # 線の確認と該当テーマなしの再判定
python3 scripts/build_viewer.py        # 画面の生成（viewer/index.html）
cp viewer/index.html index.html
```

Jev は、TypeSafe の API キーと、`jev.call(state, questions)` を持つ小さなクライアント（`~/.codex/skills/jev/scripts/jev.py` に置く想定）で呼び出す。API キーは環境変数 `TYPESAFE_API_KEY` か `~/.config/typesafe/env` から読む。OpenAlex への問い合わせには環境変数 `OPENALEX_MAILTO` に連絡先メールを入れる。本文（`data/fulltext/`）と Jev の生応答は容量と著作権のためリポジトリに含めていない。判定は処理済みの論文を飛ばすので、途中で止まっても再実行すれば続きから進む。`followup_jev.py` は実行のたびに全件を判定し直す。

## 限界

- 分類はすべて機械判定で、人手の確認を経ていない。例外は、テーマ未分類として残った5本と、該当テーマなしの再判定の採否で、いずれも Claude（生成AI）が題名・抄録と照合して決めた。この5本のうち4本は、割り当てたテーマを Jev が支持しなかった。
- 336本は題名と抄録だけで判定した。本文の方法欄にしか書かれていない情報（データ源、解析データの年）は取りこぼしがありうる。
- 収集は英語名での検索による。和文誌のうち Europe PMC に収載されていないものは含まれない。
- 主曝露・主アウトカムが既存テーマに当たらない箇所が18残っている。
- 線は各論文の主曝露→主アウトカムの1組だけである。複数要因の探索や副次解析の組合せは線にしていない。
- 空欄やテーマの小ささは、その組合せの研究が存在しないことを意味しない。線は解析の組合せを示すもので、因果関係は示さない。

## 謝辞

本研究は JSPS 科研費 JP23K16359（若手研究「全自治体予測モデルによるCOVID-19流行下の自殺要因の分析」）の助成を受けたものです。

書誌と本文は Europe PMC、参考文献と被引用数は OpenAlex（CC0）から取得した。
